%ul.media-list
  - @villains.each do |villain|
    %li.media
      .media-left
        = image_tag villain.mugshot.url(:thumb), class: "media-object center-block"
      .media-body
        %h4.media-heading
          = link_to villain.name, villain
          (#{villain.generation})
        %div
          = markdown(villain.drive)
        %div
          %em
            = villain.conditions.map { |c| c.name }.join(", ")
        - if show_user
          .text-muted
            %em
              by #{link_to villain.user.name, villain.user}
.text-center
  = will_paginate @villains, renderer: BootstrapPagination::Rails
