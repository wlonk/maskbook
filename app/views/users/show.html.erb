<% provide(:title, @user.name) %>
<h1>
  <%= gravatar_for @user %>
  <%= @user.name %>
</h1>
<ul>
  <% @villains.each do |villain| %>
    <li>
      <%= link_to villain.name, villain %>
    </li>
  <% end %>
</ul>
